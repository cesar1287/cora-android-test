<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/white"
    style="@style/ToolbarCoraView">

    <ImageView
        android:id="@+id/ivToolbarArrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/pbToolBar"
        android:layout_marginStart="@dimen/largeMargin"
        android:layout_marginEnd="@dimen/largeMargin"
        android:src="@drawable/ic_keyboard_arrow_left"/>

    <ProgressBar
        android:id="@+id/pbToolBar"
        style="@style/ToolbarTrackingProgressBar"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/ivToolbarArrow"
        app:layout_constraintTop_toBottomOf="parent"
        android:layout_marginEnd="@dimen/largeMargin"
        tools:progress="33" />
</androidx.constraintlayout.widget.ConstraintLayout>